# Pack Rat — Agent Instructions

## Session Start
Load the wayfinder map from GitHub Issues (repo: Cyranth/packrat, issue #1) and load these skills:

```
skill({ name: "wayfinder" })
skill({ name: "grilling" })
skill({ name: "domain-modeling" })
skill({ name: "research" })
skill({ name: "handoff" })
skill({ name: "prototype-fast" })
skill({ name: "game-designer" })
skill({ name: "game-ui-design" })
```
